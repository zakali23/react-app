import Header from '../components/header/HeaderDesign';

export default function Contact (history) {

    return(
        <div>
            <Header {...history}/>
            <h1>Contact</h1>
        </div>
    )
}